/*
package com.br.image.image_dispatch_service.infrastructure.persistence.config;

import com.br.image.image_dispatch_service.application.usecase.imagedispatch.DocumentDispatchUseCase;
import com.br.image.image_dispatch_service.domain.imagedispatch.repository.DocumentDispatchRepository;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class UseCaseConfig {

    @Bean
    public DocumentDispatchUseCase documentDispatchUseCase(
            DocumentDispatchRepository repository
    ) {
        return new DocumentDispatchUseCase(repository);
    }
}*/
